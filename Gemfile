source 'https://rubygems.org'

gem 'jekyll', '~> 4.3.0'
gem 'webrick', '~> 1.8'

# Required for Ruby 3.4+
gem 'csv'
gem 'erb'
gem 'base64'
gem 'logger'

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'jekyll-gist'
  gem 'jekyll-paginate'
end

# Remove github-pages gem as we're using GitHub Actions
# gem 'github-pages'
gem 'connection_pool', '2.5.0'
